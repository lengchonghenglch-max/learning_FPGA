# Debug Log

## Template

### [YYYY-MM-DD] 问题标题
- 问题描述：
- 现象：
- 原因分析：
- 解决方法：
- 验证结果：

### [2026-06-06] linetest 学习记录整理
- 问题描述：之前已经做过 linetest 学习，但没有按工程日志格式系统整理。
- 现象：知道模块大致功能，但对 `head/tail/run_tx/lineend` 的配合关系还不够清晰。
- 原因分析：此前学习偏向“能跑通”，缺少按模块功能、关键逻辑、波形、问题复盘的结构化记录。
- 解决方法：先建立 `docs/linetest.md`，按功能理解、逻辑分析、知识点、波形、问题、验证结果逐项整理。
- 验证结果：已完成 linetest 第一版学习文档骨架，下一步补充时序推演和波形分析。

### [2026-06-07] linetest 发送握手理解
- 问题描述：不清楚为什么 `tail` 不能在 `run_tx = 1` 后每拍自增。
- 现象：直觉上只要进入发送阶段，就好像可以连续读出后续字节。
- 原因分析：UART 发送器存在 `busy` 握手，只有发送器在当前拍接受了这个字节，`tail` 才能安全指向下一个字节。
- 解决方法：结合 `tx_stb`、`tx_busy` 和 `tail` 的关系分析发送握手。
- 验证结果：确认 `tail` 只应在 `tx_stb && !tx_busy` 时前进，否则会跳过未真正送入发送器的数据。
